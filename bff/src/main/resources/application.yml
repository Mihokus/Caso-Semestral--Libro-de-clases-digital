server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: bff
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

asistencia:
  url: ${ASISTENCIA_URL:http://asistencia-app:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
