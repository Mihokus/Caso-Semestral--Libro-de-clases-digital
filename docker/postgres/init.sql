-- Crea las bases de datos de cada microservicio (patron Database per Service).
-- La base 'gateway_db' la crea el contenedor con POSTGRES_DB; aqui creamos el resto.
CREATE DATABASE asistencia;
CREATE DATABASE academic_db;
CREATE DATABASE mensajeria_db;
